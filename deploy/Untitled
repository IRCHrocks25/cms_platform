In docker-compose.yml, change the route-syncer's Traefik bind mount so it writes to the dynamic root instead of the cms-custom-domains subfolder (Traefik's file provider doesn't read subdirectories). Change the host side of that one volume from /etc/dokploy/traefik/dynamic/cms-custom-domains to /etc/dokploy/traefik/dynamic, leaving the container side /traefik-dynamic and the written filename custom-domains.json unchanged. Then confirm the syncer (a) only ever writes/replaces that single custom-domains.json file and never enumerates, modifies, or deletes sibling files (dokploy.yml, middlewares.yml, traefik.yml, acme.json), and (b) writes its temp file in that same directory before renaming, so the atomic rename stays on one filesystem and fires Traefik's watch. Don't change the management command, settings, or the router JSON. Commit and push to the branch.