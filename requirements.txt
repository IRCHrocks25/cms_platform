Django==5.1.2
beautifulsoup4==4.13.3
lxml==5.4.0
pillow==10.4.0
dj-database-url==2.3.0
python-dotenv==1.0.1
httpx==0.27.2
psycopg[binary]==3.2.3
gunicorn==21.2.0
whitenoise==6.7.0
openai>=1.40,<2.0
resend>=2.0,<3.0
cloudinary>=1.40,<2.0

# OPTIONAL: server-side rendering for SPA templates fetched via the
# "Fetch from URL" button. Without these the static-fetch path still
# works, but Vite/React/Next/etc. deploys return only their shell. To
# enable, uncomment, redeploy, then exec inside the container:
#     playwright install --with-deps chromium
# Adds ~150 MB to the image; only install where the agency hands over
# JS-rendered templates.
# playwright>=1.40,<2.0
